
<!-- nubo-skills governance -->
## nubo-skills Governance

Only use `nb-*` commands from the nubo-skills catalog.
Do not install skills directly from public registries without governance review.

Work in `specs/{NNN}-{feature}/` for all pipeline artifacts.
Emit the v2 Completion Response JSON and visual summary when a command completes.
